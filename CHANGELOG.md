# 변경 로그

도서출판 민홀릭 홈페이지의 수정 내역입니다. 최신 날짜가 위에 옵니다.

- **왜** 바꿨는지를 같이 적습니다. 같은 자리를 되돌리는 실수를 막기 위해서입니다.
- 설계 원칙·주의사항은 [CLAUDE.md](CLAUDE.md), 수정 방법은 [README.md](README.md) 를 보세요.
- `커밋` 열의 해시로 `git show <해시>` 하면 그때 바뀐 코드를 볼 수 있습니다.

---

## 2026-09-11

### 도서 05 『비즈니스 엑셀 & 파워포인트』 추가 (신간)

| 내용 |
|---|
| **도서 카드 05 추가.** 기존 카드와 같은 양식 — 표지 `<img class="cover">` + `.no` + `h3` + `.sub` + 소개문 + `.meta`. 표지에 한글 제목이 없어 `h3` 는 02 번 표기를 따라 `비즈니스 엑셀 & 파워포인트` 로 정했습니다 |
| **ISBN `979-11-962091-7-9`** — 카페 글에서 확인한 값이라 `.meta` 에 넣었습니다 |
| **표지 `images/cover-fashion-business-excel-powerpoint.jpg`** (800×800, 33KB). 기존 3종과 같은 절차 — 흰 배경을 트림 → 정사각 94% 중앙 배치 → JPEG 품질 80. 원본 4000×4000 은 `패션비즈니스엑셀파워포인트.jpg` 로 루트에 보관 |
| 데스크톱 1440px · 모바일 390px 에서 렌더링 확인, 가로 넘침 0 |

**소개문은 카페 글의 책소개·목차·출판사 서평에서 옮겼습니다** — 엑셀 & 파워포인트 365,
AI 코파일럿을 쓴 프레젠테이션·패션 포트폴리오, PART 03 의 ITQ 자격증 대비까지 세 가지입니다.

**넣지 않은 것 세 가지**

- **발행년도·정가** — 카페 글에 없습니다. 서평의 "2026년 가장 핫한 AI 코파일럿" 은 발행년도
  표기가 아니므로 `2026` 을 임의로 쓰지 않았습니다. `.meta` 는 ISBN 한 칸입니다.
- **카페 글 주소** — 캡처가 모바일 카페 화면이라 주소창이 없었고, 글 하단 QR 을 디코딩해 봤지만
  카카오톡 채널 주소였습니다. 01~04 처럼 글 번호를 추측해 넣지 않고 일단 카페 메인으로 연결했다가,
  **같은 날 사용자가 알려준 글 번호 `48211` 로 교체했습니다** (아래 항목).
- **02 번 카드 변경** — 05 는 02 에 파워포인트·ITQ·AI 코파일럿을 더한 확장판 성격이고 카드에서는
  제목이 거의 같아 보이지만, ISBN 이 서로 다른 별개의 책이고 요청이 "추가" 였으므로
  **02 는 그대로 뒀습니다.** 02 를 내릴지는 사용자 결정 사항으로 남겨 뒀습니다.

### 도서 05 카페 글 주소 반영

사용자가 `m.cafe.naver.com/minholicbook/48211` 을 알려줘 카드 링크를 카페 메인에서 이 글로
바꿨습니다. **`m.` 은 떼고** `https://cafe.naver.com/minholicbook/48211` 로 넣었습니다 —
나머지 4개와 형식을 맞추기 위해서이고, `m.` 붙은 주소는 PC 에서 열어도 모바일 레이아웃이 뜹니다.
`cafe.naver.com` 은 이 세션에서도 차단되어 열어서 확인하지는 못했습니다.

### 그리드 — 값을 다시 보되 바꾸지 않음

도서가 4종 → 5종이 되면서 CLAUDE.md 가 재검토하라고 적어둔 `.books` 의
`minmax(min(330px,100%),1fr)` 을 확인했습니다. **그대로 둡니다** — 넓은 화면 내부 폭 992px,
gap 26px 기준 3열은 1042px 이 필요해 어차피 들어가지 않고, 3열로 만들려면 최소값을 310px
이하로 낮춰야 하는데 그러면 카드 다섯 개가 다 좁아져 표지가 작아집니다. 5종은 홀수라 어느
열 수에서도 딱 떨어지지 않으므로 마지막 줄에 카드 하나가 남는 편을 택했습니다.
관련 CSS 주석도 4종 기준 설명이라 같이 고쳤습니다.

## 2026-07-28

### 도서 정보 — 실제 내용으로 교체

| 시각 | 내용 | 커밋 |
|---|---|---|
| 16:47 | **도서 02~04 소개문을 실제 내용으로 교체.** 그때까지는 제목만 보고 지어낸 임시 한 줄이었습니다 (예: "패션 실무에서 필요한 엑셀 작업을 다룹니다"). 카카오톡 채널 소식 글에 실제 책소개·목차·출판사 서평이 올라와 있어 그걸로 바꿨습니다 | `446da28` |
| 16:47 | 02번 **ISBN `979-11-962091-2-4`** 추가, 히어로 저자 표기를 `박민아 · 의류학 박사` 로 보강 | `446da28` |

**일부러 넣지 않은 것 두 가지**

- **도안 개수** — 채널 글에 "36개의 힐링 만다라 & 나비 도안" 이라고 적혀 있는데, 이 문장이 두 권 공통 활용법 안에 있어 **각 권 36개인지 두 권 합쳐 36개인지 구분되지 않습니다.** 공식 사이트에 불확실한 수치를 쓰지 않으려고 뺐습니다.
- **정가** — 02·03·04 는 각 13,000원이지만 **01번 가격을 모릅니다.** 3권만 표시하면 어긋나고 가격은 자주 바뀌므로 `.meta` 는 ISBN 만 둡니다.

### 유지보수성 · 정확성

| 시각 | 내용 | 커밋 |
|---|---|---|
| 16:51 | **`tools/make-og.py` 를 저장소에 추가.** 그전까지는 임시 폴더에만 있어서, 폴더가 정리되면 팔레트를 바꿀 때마다 og.png 를 처음부터 다시 그려야 했습니다. 옮기면서 죽은 코드(히어로 도식화용 함수 6개)와 하드코딩된 절대 경로를 정리했고, **출력 PNG 5개가 기존 파일과 바이트 단위로 동일함을 확인**해 제거가 안전했음을 검증했습니다 | `6843400` |
| 16:47 | **og.png 안의 `DIGITAL FASHION DESIGN` 을 초록 → 빨강.** 같은 날 사이트에서 이 색을 바꿨는데 og.png 는 굽힌 이미지라 따라오지 않아, 카카오톡 공유 미리보기만 초록으로 남아 있었습니다 | `446da28` |
| 16:38 | **`:root` 주석이 빨강/초록 역할 교체를 반영하지 않아 거꾸로 설명하던 것 수정.** 이미 제거된 `도식화 중심선`·`TRIM` 언급도 삭제 | `d691f65` |
| 16:38 | **`og:image:alt` 정정.** "교재 판형을 치수선으로 표시한 도식화" 라고 되어 있었지만 실제 og.png 는 표지 사진 + 도서 정보입니다. 히어로를 표지로 바꿀 때 이미지는 다시 만들었는데 alt 만 옛 설명이 남아 있었습니다 | `d691f65` |
| 16:34 | README 의 "주소 확정 후 절대 URL 3곳 교체 필요" 안내 제거 — **2026-07-27 배포 때 이미 완료된 일**인데 미완료처럼 남아 있었습니다 | `16db9b6` |

### 헤더 · 히어로 타이포

| 시각 | 내용 | 커밋 |
|---|---|---|
| 11:28 | 헤더 `MINHOLIC` 을 15px → **17px** 로 한 번 더 확대. 옆 한글과의 너비 차이를 좁힘 (101px 대 95px) | `4967169` |
| 10:43 | **헤더 워드마크와 히어로 라틴의 색을 서로 교체** — `MINHOLIC` 은 빨강 → **초록**, `DIGITAL FASHION DESIGN` 은 초록 → **빨강**. 워드마크는 링크 전체의 일부라 브랜드·상호작용 성격의 초록이 맞고, 히어로 라틴은 도서를 식별하는 정적 라벨이라 인쇄 표시 계열의 빨강이 맞다는 판단입니다 | `e874a1c` |
| 10:43 | 두 글씨 크기 확대 — `.mark em` 12.5px → 15px, `.hero .latin` 15.5px → **24px** (위 한글 제목과 너비가 비슷해지도록) | `e874a1c` |

### 연락처 (Contact 카드)

| 시각 | 내용 | 커밋 |
|---|---|---|
| 12:47 | **CHAT 표시 텍스트를 `@MINHOLICBOOKS` 로 변경.** 채널 프로필의 공식 검색용 ID 입니다. ⚠ **`href` 는 `pf.kakao.com/_Gzvfxl` 그대로 유지** — `pf.kakao.com/@minholicbooks` 는 실제로 열어보면 404 입니다. 이 한 줄만 표시 텍스트와 링크 주소가 다릅니다 | `915d0ab` |
| 11:09 | CHAT·STUDY 도 메일·유튜브처럼 **주소를 그대로 표시** (`카카오톡 채널` → `pf.kakao.com/_Gzvfxl`). 네 줄의 표기 방식을 통일 | `4190b40` |
| 10:45 | **`KAKAO` 라벨을 `CHAT` 으로 변경** (채널 이름이 아니라 기능 — 문의·채팅 — 을 드러내는 라벨로), **네이버 카페(`STUDY`) 추가** | `5c23d82` |

### 모바일 대응

| 시각 | 내용 | 커밋 |
|---|---|---|
| 09:44 | **모바일에서 헤더·히어로만 화면 끝에 붙던 문제 수정.** 원인은 반응형 미구현이 아니라 `.wrap` 의 좌우 여백(`padding:0 var(--pad)`)을 **`padding` 단축 속성이 0 으로 덮은 것**이었습니다 (`.hero` 와 860px 미디어쿼리의 `.bar` 두 곳). 넓은 화면에서는 `max-width` 때문에 드러나지 않아 발견이 늦었습니다. 320~1600px 13개 폭에서 넘침 0 확인 | `d3e4c57` |

> 이 함정은 CLAUDE.md §3-5 에 따로 적어뒀습니다. `class="wrap"` 을 붙일 때마다 확인해야 합니다.

### 문서

| 시각 | 내용 | 커밋 |
|---|---|---|
| 09:30 | 확인된 사항과 보류 결정을 문서에 반영 (카페 링크 열림 확인, 포지셔닝 그대로 두기로 확정 등) | `8f4ac9b` |

---

## 2026-07-27

### 디자인 개편

| 시각 | 내용 | 커밋 |
|---|---|---|
| 15:56 | **도서 표지 3종 반영 + 히어로를 도식화 SVG → 표지 사진으로 교체.** 출판사 사이트에서 방문자가 가장 먼저 확인하고 싶은 것은 **실물 책**이라는 판단입니다. 원본 4000×4000 을 트림·정사각 배치 후 히어로 1100px / 카드 800px 로 가공 | `619b819` |
| 15:56 | **영문 전면 대문자 + 고딕화.** 라틴 디스플레이용 **Bodoni Moda italic 을 전부 제거**하고 Google Fonts 요청에서도 빼 폰트 하나를 덜 받습니다. 예외 — 이메일과 유튜브 핸들은 오독을 유발하므로 소문자 유지 | `619b819` |
| 15:31 | **로고 2종 적용.** 검정 단색 로고는 헤더·소개·OG·파비콘 전부에, 네온 로고는 **유튜브 채널 타일 한 곳만**. 핑크 네온이 흰 바탕 + 빨강·초록 팔레트와 충돌하므로 테두리 안에 가둬 "채널 아트"로 읽히게 했습니다 | `6b338a0` |
| 15:17 | **봄 팔레트로 개편** — 회색 캔버스 → **흰 종이 바탕**, 포인트 컬러 **빨강·초록**. 크리스마스처럼 보이지 않게 역할을 분리했습니다: 빨강 = 인쇄 재단 표시 계열(정적), 초록 = 브랜드·상호작용(호버 전부). 개편 전 `--muted` 는 회색 위에서 3.0:1 로 **WCAG AA 미달**이었는데 이때 고쳐졌습니다 | `29f3923` |

### 콘텐츠

| 시각 | 내용 | 커밋 |
|---|---|---|
| 14:58 | 출간 도서를 **실제 4종으로 교체** (제목·부제·카페 링크). 『비즈니스 엑셀』만 표지가 없어 그 카드만 내용 높이에 맞추는 처리(`:has()`) 추가 | `900c5b4` |
| 14:52 | **연락처를 메일·카카오톡 채널·유튜브로 정리.** 전화번호·주소·견본 도서 신청 안내는 **넣지 않기로 확정** | `c3bbced` |

### 배포

| 시각 | 내용 | 커밋 |
|---|---|---|
| 14:39 | 배포 완료 기록. 페이지·og.png·파비콘 3종 모두 200 응답, 절대 URL 3곳이 실제 주소와 일치함을 확인 | `8b724dd` |
| 14:29 | 사이트 주소를 **`https://minholicbook.github.io/`** 로 확정하고 `canonical`·`og:url`·`og:image` 반영. 조직 이름을 단수 `minholicbook` 으로 한 것은 기존 네이버 카페 주소와 표기를 맞추기 위해서입니다 | `1e442f3` |
| 14:19 | 문서: 파일 구성·GitHub 주소 방식·작업 현황 갱신 | `46d76a1` |
| 14:18 | **홈페이지 초기 버전** + 파비콘 3종 + apple-touch-icon + og.png. 파비콘은 크기별로 아트를 나눴습니다 — 16px 은 마크 안쪽 글자가 뭉개져 **실루엣 버전**을 씁니다 | `879efbe` |

---

## 아직 결정되지 않은 것

진행 상황은 CLAUDE.md §8 "다음 할 일" 이 기준입니다. 그중 사장님 결정을 기다리는 항목만 옮겨둡니다.

| 항목 | 상태 |
|---|---|
| **소식 3건이 가상 샘플** | `href="#"` 이라 클릭해도 아무 일도 일어나지 않습니다. 실제 소식으로 교체하거나 섹션을 내려야 합니다. **카카오채널에는 개별 글 주소가 없어**(2026-07-28 확인) 카페 글 주소가 필요합니다 |
| 『비즈니스 엑셀』 표지 | 표지를 구할지, 카드를 뺄지 미정 |
| 출판사 소개 3문단 | 톤만 잡은 초안 |
| 문의 폼 도입 | 도입하면 **개인정보처리방침 페이지가 필수** |
| 도서 03·04 ISBN·발행년도 | 미확인 |
| 컬러링 북 도안 개수 | 각 권 36개인지 합쳐 36개인지 미확인 |
| 도서가 4종보다 많은지 | 카페 목록 화면 아래가 잘려 있어 미확인 |
| **카카오채널의 "웹사이트 보러가기"** | 현재 옛 사이트(`minholic.imweb.me`)를 가리킵니다. **홈페이지 완성 후 바꾸기로 결정** (2026-07-28) |
