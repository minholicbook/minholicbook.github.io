# 도서출판 민홀릭 홈페이지 — 작업 인계 문서

> 이 파일은 Claude Code에서 작업을 이어가기 위한 컨텍스트 문서입니다.
> 저장소 루트에 `CLAUDE.md`로 두면 Claude Code가 자동으로 읽습니다.

---

## 1. 프로젝트 개요

**무엇을** — 도서출판 민홀릭(minholic books)의 공식 홈페이지
**분야** — 패션 전공 교재 출판 (일러스트레이터·포토샵 기반 디지털 패션 디자인)
**형태** — 빌드 과정 없는 단일 파일 정적 사이트 (`index.html`)
**배포** — GitHub Pages
**언어** — 한국어

### 페이지의 목적 (우선순위 순)

1. 방문자가 어떤 교재가 있는지 보고, 자기 강의/실무에 맞는지 판단
2. 교재 채택·실습 파일·재고 문의로 연결
3. 출판사 소식 전달

### 방문자

- 패션 전공 학생 (교재 구매)
- 대학 교수·강사 (교재 채택 검토)
- 현업 패션 디자이너 (도식화·텍스타일 실무 참고서)

### 연락 수단 (2026-07-27 확정)

| 항목 | 값 |
|---|---|
| 이메일 | `minholicbooks@gmail.com` |
| 카카오톡 채널 | https://pf.kakao.com/_Gzvfxl |
| 유튜브 | https://www.youtube.com/@minholicbooks2025 |

**전화번호와 주소는 노출하지 않습니다. 견본 도서 신청 안내도 넣지 않습니다.**
소개문 마지막 문단과 Contact 카드에서 모두 뺐으니, 문구를 다시 쓸 때도 되살리지 마세요.

외부 링크에는 `target="_blank" rel="noopener"`를 붙였습니다. 사이트에서 밖으로 나가는 링크는
이 두 개뿐입니다 (Google Fonts CDN 제외).

---

## 2. 현재 파일

```
/
├── index.html            # 홈페이지 본체 (HTML + CSS + JS 인라인, 외부 의존성은 Google Fonts뿐)
├── images/
│   ├── logo-mark.png       # 원피스 마크만 (투명). 헤더·OG·아이콘용
│   ├── logo.png            # 마크 + 민홀릭 (투명). 소개 섹션용
│   ├── logo-silhouette.png # 외곽을 채운 실루엣. 16px 파비콘 전용
│   ├── minholic-neon.jpg   # 네온 로고 (유튜브 채널 타일)
│   ├── cover-digital-fashion-design.jpg  # 표지. 히어로 + 도서 01
│   ├── cover-mandala.jpg                 # 표지. 도서 03
│   └── cover-butterfly.jpg               # 표지. 도서 04
├── minholic_logo1.jpg    # 원본 로고 (CMYK, 1267×1578). 페이지에서 직접 참조하지 않음
├── minholic_logo2.jpg    # 원본 네온 로고. 페이지에서 직접 참조하지 않음
├── 디지털패션디자인.jpg    # 원본 표지 (4000×4000). 페이지에서 직접 참조하지 않음
├── 힐링만다라.jpg          # 원본 표지 (4000×4000)
├── 홀릭컬러북2(나비).png   # 원본 표지 (4000×4000, RGBA)
├── favicon-16.png        # 실루엣 버전 (마크가 뭉개지는 크기)
├── favicon-32.png        # 마크
├── favicon-48.png        # 마크
├── apple-touch-icon.png  # iOS 홈 화면 아이콘 180×180
├── og.png                # 소셜 공유 미리보기 1200×630
├── README.md             # 배포 방법, 수정 위치 안내
├── .gitignore
├── .gitattributes        # 저장소 안 줄바꿈은 LF로 고정
└── CLAUDE.md             # 이 문서
```

마크업은 `index.html` 한 파일에 전부 들어 있습니다. 빌드 툴, npm, 프레임워크 없음.

git 저장소는 초기화되어 있고 첫 커밋(`main` 브랜치)까지 되어 있습니다. 원격은 아직 없습니다.

---

## 3. 디자인 시스템 — **작업 시 반드시 유지**

새 섹션을 추가하거나 수정할 때 아래 토큰과 컨셉을 따라 주세요. 임의로 새 색이나 서체를 도입하지 않습니다.

### 3-1. 컨셉

**"흰 종이 위의 도식화 스펙 시트 + 새잎 초록"** (2026-07-27 개편)

바탕을 흰색으로 바꾸고 포인트 컬러를 빨강·초록으로 잡았습니다.
원래는 일러스트레이터의 **회색 캔버스** 위에 흰 아트보드가 떠 있는 구성이었고
액센트가 빨강·파랑(패턴 제도용 색연필)이었습니다. 개편으로 회색 캔버스 은유는 없어졌고,
**도식화 스펙 시트**의 시각 언어(재단 표시, 치수선, 모노 라벨)가 정체성의 중심으로 남았습니다.

- 페이지 바탕 = 흰 종이
- 콘텐츠 블록 = 판면 (`.artboard`). 바탕이 흰색이므로 **테두리 1px 이 유일한 구분선**
- 도서 카드 모서리 = 인쇄 **재단 표시(trim mark)**, 빨간 ㄱ/ㄴ 자 (CSS `::before` / `::after`)
- 히어로 = 대표 도서 **『디지털 패션 디자인』 표지 사진** (2026-07-27 변경)
- 옅은 초록 `--wash` = 흰 바탕에서 판면만으로 구분이 약한 곳(연락처 카드, 소식 호버)에 사용

**히어로에 있던 도식화 SVG는 제거했습니다.** 원래는 판형 치수선(188×257)이 달린 민소매 원피스
도식화였고 그것이 컨셉의 핵심이었습니다. 실제 책 표지로 바꾼 이유는 출판사 사이트에서
방문자가 가장 먼저 확인하고 싶은 것이 **실물 책**이라는 판단입니다.
재단 표시(도서 카드 모서리)와 모노 라벨은 그대로 남아 스펙 시트 인상을 유지합니다.

### 3-2. 컬러 토큰

`index.html` 최상단 `:root`에 정의되어 있습니다.

| 변수 | 값 | 용도 |
|---|---|---|
| `--canvas` | `#FFFFFF` | 페이지 바탕 |
| `--artboard` | `#FFFFFF` | 콘텐츠 판면 (테두리로만 구분) |
| `--wash` | `#F3F8F1` | 옅은 초록 바탕 — 연락처 카드, 소식 호버 |
| `--ink` | `#17181C` | 제목, 주요 선 |
| `--body` | `#3E453F` | 본문 문단 |
| `--muted` | `#697169` | 라벨, 보조 텍스트, 치수선 |
| `--rule` | `#E3E9E0` | 구분선, 테두리 |
| `--red` | `#CE2E22` | 재단 표시, `TRIM`, 도서 번호, 히어로 라틴 |
| `--green` | `#35803E` | 헤더 워드마크, 태그, 호버, 포커스 링 |

### 3-2-1. 빨강과 초록의 역할을 섞지 말 것

빨강·초록을 같은 비중으로 쓰면 **크리스마스처럼** 보입니다. 그래서 역할을 갈라놨습니다.

| | 쓰는 곳 | 성격 |
|---|---|---|
| **빨강** | 재단 표시, 도서 카드 번호, 히어로 라틴 `DIGITAL FASHION DESIGN` | 정적·소량. 인쇄 표시 계열 |
| **초록** | 헤더 워드마크 `MINHOLIC`, 소식 태그, 모든 호버, 포커스 링 | 브랜드·상호작용. 초록이 지배적 |

개편 전에는 호버가 빨강이었습니다. 초록으로 옮긴 덕에 빨강이 인쇄 표시로만 남아
두 색이 같은 비중으로 부딪히지 않습니다. **새 요소의 호버·강조는 초록을 쓰세요.**

**2026-07-28 재조정** — 헤더 워드마크(`MINHOLIC`)와 히어로 라틴(`DIGITAL FASHION DESIGN`)의
색을 서로 바꿨습니다. 헤더 워드마크는 링크 전체(`.mark`)의 일부라 브랜드·상호작용 성격의
초록이 더 맞고, 히어로 라틴은 도서 번호처럼 특정 도서를 식별하는 정적 라벨이라 빨강이
더 맞습니다. 동시에 두 글씨 크기도 키웠습니다 — 헤더 `MINHOLIC`은 옆 한글(`도서출판 민홀릭`)과
비슷한 크기로, 히어로 라틴은 위 한글 제목(`h1`)과 너비가 비슷해지도록 (`.mark em` 12.5px→15px,
`.hero .latin` 15.5px→24px).

### 3-2-2. 대비 (WCAG AA)

`--muted`는 **흰 바탕과 `--wash` 두 곳 모두**에서 쓰이므로 둘 다 통과해야 합니다.
개편 초안의 `#6E766E`는 wash 위에서 4.35:1로 미달이라 `#697169`로 내렸습니다.

| 색 | 흰 바탕 | wash 위 |
|---|---|---|
| `--ink` | 17.7:1 | — |
| `--body` | 9.9:1 | — |
| `--muted` | 5.0:1 | 4.7:1 |
| `--red` | 5.2:1 | — |
| `--green` | 4.9:1 | 4.5:1 |

참고로 **개편 전 `--muted`는 회색 캔버스 위에서 3.0:1로 AA 미달**이었습니다. 개편으로 고쳐졌습니다.

### 3-2-4. 로고 (2026-07-27 적용)

원본 두 개를 받아 성격에 맞게 나눠 적용했습니다.

| 원본 | 성격 | 적용한 곳 |
|---|---|---|
| `minholic_logo1.jpg` | 검정 단색. 원피스 마크 + 한글 `민홀릭` 세로 조합 | 헤더, 소개 섹션, OG 이미지, 파비콘 전부 |
| `minholic_logo2.jpg` | 네온 핑크 + 어두운 보케 사진 | 소개 섹션 우측의 유튜브 채널 타일 **한 곳만** |

**logo1 을 세로/마크로 쪼갠 이유** — 원본이 세로 조합(1267×1578, 세로가 더 김)입니다.
62px 헤더 바에 세로 로고를 넣으면 폭이 30px밖에 안 돼 아무것도 안 보입니다.
그래서 원피스 마크(가로세로 비슷함)만 떼서 헤더·아이콘에 쓰고,
세로 전체 조합은 공간이 넉넉한 소개 섹션에 씁니다.

**투명 PNG 로 변환한 이유** — 원본이 JPG라 투명도가 없고, 검정 선 주변에 압축 잡티가 생깁니다.
밝기를 알파로 뒤집어(검정→불투명, 흰색→투명) 잉크 색을 `--ink`로 다시 칠했습니다.
원본 919KB → 마크 24KB.

**네온 로고를 한 곳에만 쓴 이유** — 핑크 네온과 어두운 사진은 흰 바탕 + 빨강·초록 팔레트와
정면으로 충돌합니다. 그래서 테두리 안에 가둬 **"유튜브 채널 아트"** 로 읽히게 하고,
링크 목적을 줘서 장식이 아니라 기능이 되게 했습니다. 페이지의 유일한 사진 요소입니다.
색을 더 퍼뜨리면 팔레트가 무너지니 **다른 곳에 확장하지 마세요.**

### 3-2-5. 파비콘은 크기마다 상세도가 다릅니다

로고 마크를 16px로 줄이면 안쪽 `MINI` 글자가 뭉개져 회색 덩어리가 됩니다.
그래서 크기별로 다른 아트를 씁니다.

| 파일 | 아트 | 이유 |
|---|---|---|
| `favicon-16.png` | `logo-silhouette.png` (외곽 채움) | 16px에서 형태가 남는 유일한 버전 |
| `favicon-32/48.png`, `apple-touch-icon.png` | `logo-mark.png` (선 그림) | 32px 이상은 마크가 읽힘 |

실루엣은 마크의 알파에서 테두리 쪽 배경을 홍수 채우기(flood fill)로 지우고
남은 영역(잉크 + 내부 흰 공간)을 통째로 칠해 만들었습니다.

**기존 `favicon.svg`(판면 + 재단 표시 브래킷)는 삭제했습니다.** 브라우저가 SVG 아이콘을
PNG보다 우선하므로, 남겨두면 로고 파비콘이 절대 표시되지 않습니다.
벡터 로고 파일이 생기면 SVG 파비콘으로 되돌리는 게 좋습니다.

### 3-3. 타이포그래피

| 역할 | 서체 | 사용처 |
|---|---|---|
| 본문·제목 | **IBM Plex Sans KR** (300–700) | 한글 전반, UI, **영문 전부** |
| 라벨·수치 | **IBM Plex Mono** (400–500) | 섹션 라벨, ISBN, 날짜, 스펙값 |

Google Fonts CDN에서 불러옵니다.

### 영문 표기 규칙 (2026-07-27 결정)

- **영문은 모두 대문자**로 씁니다. `MINHOLIC`, `DIGITAL FASHION DESIGN`, `PUBLICATIONS` 등.
- **흘림체(이탤릭·세리프)를 쓰지 않습니다.** 고딕(IBM Plex Sans KR)으로 통일합니다.
- 대문자는 뭉쳐 보이므로 **자간을 넉넉히** 줍니다 (`letter-spacing` 0.11~0.18em).

원래 라틴 디스플레이용으로 **Bodoni Moda italic** 을 썼습니다 (워드마크, 히어로 라틴, 도서 부제).
2026-07-27에 전부 제거하고 Google Fonts 요청에서도 빼서 폰트 하나를 덜 받습니다.

**예외 두 곳 — 대문자로 바꾸지 않았습니다.**

| 항목 | 값 | 이유 |
|---|---|---|
| 이메일 | `minholicbooks@gmail.com` | 메일 주소를 대문자로 표기하면 오독·오타를 유발합니다 |
| 유튜브 핸들 | `@minholicbooks2025` | 핸들은 표기 그대로 보여야 채널을 알아볼 수 있습니다 |

`.contact dt`(MAIL/KAKAO/YOUTUBE)와 `.label`은 CSS `text-transform:uppercase`로도 대문자가
되지만, 복사해 붙일 때도 대문자가 유지되도록 **HTML 원문 자체를 대문자로** 적었습니다.

`og.png` 는 웹폰트를 쓸 수 없어 시스템 폰트로 대체합니다 (§6 참고).

### 3-4. 지켜야 할 품질 기준

- 모바일 대응 (`860px` 브레이크포인트 하나)
- `:focus-visible` 키보드 포커스 링 (초록)
- `prefers-reduced-motion: reduce` 시 모든 애니메이션·부드러운 스크롤 비활성
- 애니메이션은 스크롤 진입 시 `.rise` 페이드업 하나뿐 — **추가하지 말 것.** 효과가 늘어나면 지금의 절제된 인상이 무너집니다

### 3-5. `.wrap` 의 좌우 여백을 덮지 말 것 (2026-07-28 버그 수정)

`.wrap` 은 `padding:0 var(--pad)` 로 좌우 여백을 만듭니다. 여기에 `.wrap` 과 같은 요소에
**`padding` 단축 속성을 쓰면 좌우 값이 0 으로 덮입니다.** 넓은 화면에서는 `max-width:1120px`
+ `margin:0 auto` 때문에 남는 공간이 여백처럼 보여 문제가 드러나지 않고, **좁은 화면에서만
그 블록이 화면 끝에 딱 붙습니다.**

실제로 두 곳이 이 문제였습니다.

| 위치 | 잘못된 코드 | 증상 |
|---|---|---|
| `.hero` | `padding:clamp(40px,7vw,84px) 0 clamp(48px,8vw,96px)` | 히어로 전체가 좌우 여백 0 |
| `.bar` (860px 미디어쿼리) | `padding:14px 0` | 모바일 헤더 로고·nav 가 화면 끝에 붙고 `문의` 가 잘려 보임 |

아래 섹션(`#books` 등)은 `<div class="wrap">` 만 써서 정상이었기 때문에,
**헤더·히어로만 왼쪽으로 튀어나와 페이지가 어긋나 보였습니다.**

→ 세로 여백은 `padding-top` / `padding-bottom` 으로 따로 지정하세요.
새 블록에 `class="wrap"` 을 붙일 때마다 이 점을 확인해야 합니다.

`.specs` 는 항목이 4개라 `minmax(min(160px,100%),1fr)` 로 두어 2열 → 1열로만 떨어지게
했습니다. 다만 600~800px 구간에서는 3열이 되어 `설립` 이 아래 줄에 홀로 남습니다.
브레이크포인트를 하나 더 늘리지 않으려고 그대로 뒀습니다.

---

## 4. 코드 구조

`index.html` 안의 위치입니다.

| 영역 | 셀렉터 / 위치 |
|---|---|
| 컬러·서체 토큰 | `<style>` 최상단 `:root` |
| 헤더·내비게이션 | `<header>` → `.bar`, `nav` |
| 히어로 표지 이미지 | `<figure class="artboard sheet">` 내부 `<img>` |
| 히어로 스펙 목록 | `<dl class="specs">` |
| 도서 표지 | 각 `.book` 안 첫 `<img class="cover">` |
| 도서 카드 | `<section id="books">` → `<a class="artboard book">` (블록 복사해서 추가) |
| 소식 목록 | `<section id="news">` → `.news` 안의 `<a>` |
| 출판사 소개 | `<section id="about">` → `.about` (좌: 로고 `.brandmark` + 문단 / 우: `.side`) |
| 연락처 | `.side` 안 `<div class="artboard contact" id="contact">` 내부 `<dl>` |
| 유튜브 채널 타일 | `.side` 안 `<a class="artboard chan">` |
| 헤더 로고 | `.mark` 안 첫 `<img>` |
| 스크롤 등장 스크립트 | 문서 하단 `<script>` (IntersectionObserver) |

### 도서 카드가 `<article>` 이 아니라 `<a>` 인 이유

카드 전체가 네이버 카페 소개글로 나가는 링크입니다. 원래도 `.book:hover` 에 들어올림 효과가
있어서 눌리는 것처럼 보였는데 실제로는 눌리지 않았습니다. 링크로 바꿔 그 불일치를 없앴습니다.
`<a>` 는 HTML5 에서 안에 블록 요소를 담을 수 있으므로 `.meta` `<div>` 를 넣어도 유효합니다.

**주의** — `.book p` 의 `flex:1` 은 설명 문단만 남는 공간을 흡수하게 하려는 것인데,
`.no` 와 `.sub` 도 `<p>` 라서 같이 걸립니다. 그래서 둘에 `flex:none` 을 줬습니다.
이걸 빼면 `.meta` 가 없는 카드에서 번호·부제 아래가 벌어집니다
(카드들은 그리드에서 가장 큰 카드 높이로 늘어납니다).

---

## 5. 콘텐츠 상태 — 확정 vs 임시

### 확정된 정보 (웹 검색으로 확인)

- 출판사명: 도서출판 민홀릭 / minholic books
- 대표 도서: 『Digital Fashion Design 디지털 패션 디자인』
- 2020 개정판, 패션 전공 교재
- 일러스트레이터 CC + 포토샵 CC 기반, 기초 기능부터 실무 활용까지 실습 구성
- ISBN 979-11-962091-1-7
- 저작권 표기 기준 2017년부터 활동
- 기존 사이트: imweb 기반 (`minholic.imweb.me`)
- 카카오톡 플러스(채널)로 문의 받는 중

### 임시값 — 반드시 교체 필요

| 항목 | 현재 값 |
|---|---|
| 사이트 절대 URL 3곳 | `https://minholicbook.github.io/` — 확정. 커스텀 도메인을 붙이면 `canonical`, `og:url`, `og:image` 3곳을 다시 교체 |
| 도서 02~04 소개문 | 제목을 한 줄로 풀어 쓴 **임시 문구** — 실제 소개문으로 교체 필요 |
| 도서 02~04 `.meta` | ISBN·발행년도 **추후 등록 예정** (2026-07-28 확인) — 행 자체를 넣지 않았습니다. 01번만 있습니다 |
| 도서 카드 카페 링크 4개 | URL 형식을 추정했으나 **실제로 열림을 확인함** (2026-07-28) |
| 소식 3건 | **가상의 샘플**, `href="#"` |
| 출판사 소개 3문단 | 톤만 잡은 초안 — 실제 문구로 교체 권장 |

### 출간 도서 4종 (2026-07-27 반영)

사용자가 보여준 네이버 카페 도서 목록 화면에서 옮긴 것입니다.
`cafe.naver.com` 은 도구에서 **정책으로 차단**되어 본문을 직접 읽지 못했습니다.
그래서 제목과 게시글 번호만 확보했고, 소개문·ISBN·판형·발행년도·가격은 확인하지 못했습니다.

| # | 제목 | 부제 | 카페 글 | 표지 |
|---|---|---|---|---|
| 01 | 디지털 패션 디자인 | DIGITAL FASHION DESIGN | 46952 | 있음 |
| 02 | 비즈니스 엑셀 | FASHION BUSINESS EXCEL | 46953 | **없음** |
| 03 | 홀릭 컬러링 북 힐링 만다라 | HOLIC COLORING BOOK MANDALA | 46954 | 있음 |
| 04 | 홀릭 컬러링 북 II 나비 | HOLIC COLORING BOOK II BUTTERFLY | 46955 | 있음 |

04번 제목에 `II` 가 붙는 것은 표지 실물 표기가 `홀릭 컬러링 북 II` 이기 때문입니다.
01번 저자 `박민아` 는 표지의 "박민아 지음" 에서 확인한 값입니다.

**링크 URL은 추정입니다.** 카페 목록 주소는 신형(`cafe.naver.com/f-e/cafes/28957615/menus/28`)
이었지만, 카드에는 구형 슬러그 형식 `cafe.naver.com/minholicbook/<번호>` 를 썼습니다.
슬러그 `minholicbook` 은 이 문서 §9에 기록돼 있던 `cafe.naver.com/minholicbook/46952` 와
화면의 46952 번이 일치하는 것으로 뒷받침했을 뿐, 실제로 열어 확인하지는 못했습니다.
안 열리면 신형 형식 `cafe.naver.com/f-e/cafes/28957615/articles/<번호>` 로 바꾸면 됩니다.

**목록 화면 맨 아래가 잘려 있었습니다.** 컬러링 북이 한 권 더 보였으니 도서가 4종보다
많을 수 있습니다. 확인해서 추가해야 합니다.

### 표지 이미지 처리 (2026-07-27)

원본은 모두 4000×4000 이고 흰 배경에 책 목업이 놓인 사진입니다. 웹용으로 이렇게 가공했습니다.

1. 흰 배경이 아닌 영역(책 + 그림자)의 바운딩박스로 **트림** — 원본 여백이 너무 넓습니다
2. 정사각 캔버스 94% 크기로 **중앙 배치** — 책 모양이 서로 달라도 시각적 무게를 맞춥니다
3. 히어로용 1100px, 카드용 800px, JPEG 품질 80 (각 57~97KB)

배경을 흰색으로 남긴 이유는 페이지·판면이 모두 `#FFFFFF` 라 경계가 보이지 않고
책이 떠 있는 것처럼 읽히기 때문입니다. 투명 PNG 로 만들 필요가 없습니다.

**표지 없는 카드 처리** — 『비즈니스 엑셀』만 표지가 없습니다. 그리드에서 카드는 기본적으로
같은 행의 최대 높이까지 늘어나므로, 표지 없는 카드 아래가 **수백 px 비어 버립니다.**
그래서 `.book:not(:has(.cover)){align-self:start}` 로 그 카드만 내용 높이에 맞췄습니다.

**『비즈니스 엑셀』 자체를 목록에서 내릴지는 2026-07-28 기준 미정입니다.** 표지를 구해서
채울지, 카드를 아예 뺄지 결정되지 않았습니다. 결정 전까지는 지금처럼 표지 없는 카드로 둡니다.
`:has()` 를 못 읽는 구형 브라우저는 예전처럼 늘어나기만 하므로 깨지지 않습니다.

**그리드 폭 함정** — `minmax(330px,1fr)` 로 두면 화면이 330px 보다 좁을 때 트랙이 줄지 못해
가로 스크롤이 생깁니다(320px 에서 45px 넘침). `minmax(min(330px,100%),1fr)` 로 감싸야 합니다.

### 포지셔닝 (2026-07-28 결정 — 그대로 둔다)

히어로 `h1` 주변 문구, `og:description`, `<meta name="description">` 이 모두
**"패션 전공 교재"** 만 이야기합니다. 실제 출간 목록에는 컬러링 북 2종이 있어
패션과 무관하지만, **문구를 넓히거나 섹션을 분리하지 않고 그대로 두기로 결정했습니다.**
컬러링 북이 목록에서 이질적으로 보이는 것은 감수합니다. 나중에 다시 논의되기 전까지
이 문구를 임의로 넓히지 마세요.

---

## 6. 배포 (GitHub Pages)

### 배포 완료 (2026-07-27)

| 항목 | 값 |
|---|---|
| GitHub 조직 | `minholicbook` — 개인 계정(`asmendless-blip`) 소유의 무료 조직 |
| 저장소 | `minholicbook.github.io` (Public) |
| 주소 | **https://minholicbook.github.io/** — 배포됨, HTTPS 강제 적용 |

`<조직>.github.io` 이름의 저장소는 push하면 GitHub가 Pages를 **자동으로 켭니다.**
Settings → Pages에서 따로 Source를 지정할 필요가 없었습니다.

배포 후 확인한 것: 페이지·og.png·파비콘 3종 모두 200 응답, MIME 정상,
`canonical`/`og:url`/`og:image` 가 실제 주소와 일치, Google Fonts 3종 응답 정상.

이름을 단수(`minholicbook`)로 한 것은 기존 네이버 카페 주소(`cafe.naver.com/minholicbook`)와
표기를 맞추기 위해서입니다. 워드마크·OG 이미지의 `MINHOLIC BOOKS` 표기와는 다르지만 의도된 것입니다.

`<이름>.github.io` 형식에서 `<이름>`은 **계정 또는 조직 이름**입니다. 저장소 이름만 바꿔서는 짧은
주소가 나오지 않고, 소유 계정 이름이 그대로 주소가 됩니다. 저장소 이름도 `<이름>.github.io`로
똑같이 맞춰야 합니다 (다른 이름이면 `https://minholicbook.github.io/<저장소>/`처럼 경로가 붙습니다).

### 이후 수정 반영 방법

`main`에 push하면 1~2분 뒤 자동 반영됩니다. 별도 빌드나 배포 명령이 없습니다.

```bash
git add -A && git commit -m "수정 내용" && git push
```

**커스텀 도메인**
- 루트에 `CNAME` 파일 (도메인만 한 줄)
- A 레코드: `185.199.108.153`, `185.199.109.153`, `185.199.110.153`, `185.199.111.153`
- www: CNAME → `<계정>.github.io`
- Settings → Pages에서 `Enforce HTTPS`

---

## 7. 논의된 기술 결정

### 왜 정적 사이트인가

GitHub Pages는 정적 파일만 서빙합니다. 대신 종속성이 없어 Netlify, Cloudflare Pages, 일반 웹호스팅 어디로든 파일만 옮기면 그대로 동작합니다. 비용 0원.

**불가능한 것** — 서버 사이드 처리, 관리자 로그인, DB, 결제/장바구니

### 소식(블로그) — 미결정

**소식 섹션 자체를 페이지에서 내릴지 2026-07-28 기준 고민 중입니다.** 지금 있는 3건은
가상 샘플(`href="#"`)이라 실제 공지로 바꾸거나, 아예 섹션을 빼는 것 중 아직 정해지지 않았습니다.
남기기로 결정되면 아래 관리 방식도 같이 정해야 합니다.

| 방식 | 내용 |
|---|---|
| 현행 (하드코딩) | 새 글마다 `index.html` 직접 수정. 글이 드물면 충분 |
| **Jekyll** | GitHub Pages 내장. `_posts/`에 마크다운 추가하면 목록 자동 생성. 지금 디자인 그대로 이식 가능 |
| 카페 링크 | 글은 네이버 카페에 쓰고 소식 항목에서 링크만 연결. 관리 부담 최소 |

### 댓글 — 미결정, 도입 회의적

| 방식 | 평가 |
|---|---|
| giscus / utterances | 무료·무광고지만 **GitHub 로그인 필수** → 패션 전공 방문자에겐 사실상 사용 불가 |
| Disqus | 진입장벽 낮지만 무료 플랜 광고, 로딩 무거움, 국외 이전 고지 필요 |
| Cusdis | 가볍고 로그인 불필요, 승인제. 무료 티어는 소규모 기준 |
| Supabase / Firebase 직접 구현 | 자유도 최고, 무료 한도 충분. 스팸 필터·관리 화면 직접 구현 필요 |

**결론 방향** — 출판사 사이트 방문자가 남기고 싶은 건 대부분 공개 댓글이 아니라 "재고 있나요", "교재 채택 문의합니다" 같은 **1:1 문의**입니다. 따라서 다음 조합을 권장했습니다.

- 문의 폼 (Formspree 무료 티어 — 정적 사이트에서 동작, 제출 시 메일 수신)
- 카카오톡 채널 버튼을 눈에 띄게 배치
- 댓글이 필요한 소식은 네이버 카페 글로 링크

---

## 8. 다음 할 일

- [x] 연락처 확정 — 메일·카카오톡 채널·유튜브 (전화·주소는 넣지 않음) — 2026-07-27
- [x] 도서 목록 실제 4종으로 교체 (제목·부제·카페 링크) — 2026-07-27
- [x] 도서 카드 카페 링크 4개 실제로 열리는지 확인 — **잘 열림 확인** (2026-07-28)
- [ ] 잘린 목록 확인 — 도서가 4종보다 많은지 (스크린샷 목록 화면 맨 아래가 잘려 있었음, 미확인)
- [x] 저자 표기 `박민아` 확인 (2026-07-28)
- [ ] 도서 02~04 ISBN·발행년도 — **추후 등록 예정** (2026-07-28 확인). 등록되면 `.meta` 행 추가
- [x] 포지셔닝 결정 — **그대로 두기로 확정** (2026-07-28). §5 "포지셔닝" 참고
- [ ] 출판사 소개 문구 확정
- [ ] 소식 섹션 — **존치 여부 고민 중** (2026-07-28). 내리면 §7 "소식(블로그)" 관리 방식 논의도 보류
- [x] 도서 표지 이미지 추가 결정 (2026-07-27 반영, `images/` 폴더 + 카드 내 `<img>`)
- [ ] 문의 폼 도입 여부 결정
- [x] favicon, Open Graph 이미지 추가 — 2026-07-27
- [ ] 개인정보처리방침 페이지 (문의 폼이나 댓글 도입 시 **필수**)
- [x] GitHub 조직 이름 확정 (`minholicbook`) + `<head>` 절대 URL 3곳 반영 — 2026-07-27
- [x] 원격 저장소 연결 + push + Pages 배포 — 2026-07-27
- [x] 봄 팔레트로 개편 — 흰 바탕, 포인트 컬러 빨강·초록 — 2026-07-27
- [x] 로고 2종 적용 (헤더·소개·OG·파비콘 / 네온은 유튜브 타일) — 2026-07-27
- [x] 도서 표지 3종 반영 + 히어로를 표지 사진으로 교체 + 영문 전면 대문자·고딕화 — 2026-07-27
- [ ] 『비즈니스 엑셀』 — **표지를 구해 채울지, 카드를 뺄지 고민 중** (2026-07-28)
- [ ] 카카오톡에 링크 붙여 og.png 미리보기 확인. **팔레트 개편으로 og.png가 바뀌었으니
      이전에 공유한 적이 있으면 카카오 디버거에서 캐시를 지워야 새 이미지가 뜹니다**
- [ ] 도서 권수가 4종에서 바뀌면 `.books` 의 `minmax(min(330px,100%),1fr)` 값을 다시 볼 것
      (현재는 넓은 화면에서 2×2 로 맞아떨어지는 값)

### 파비콘 · OG 이미지에 대해

- **favicon.svg** — 옅은 초록 배경(`--wash`) 위에 판면(`--ink`)과 책등선(흰색),
  도서 카드의 재단 표시 브래킷(`--red`)을 얹은 것. 16px 탭에서도 형태가 남도록 브래킷을
  2단위 굵기로 뒀습니다. 배경을 흰색이 아니라 `--wash`로 한 이유는 두 가지입니다 —
  흰 탭 배경에서 아이콘 경계가 사라지지 않게, 그리고 새 팔레트의 초록을 아이콘에도 남기려고.
- **og.png** — 히어로 SVG(viewBox 400×470)의 좌표를 **그대로 옮겨** 그렸습니다.
  치수선·재단 표시·도식화 베지에 곡선이 사이트 히어로와 동일합니다.
  왼쪽 타이포는 사이트 `h1`("화면 위에서 옷을 그리는 법")과 같은 문구를 씁니다.
  바탕은 `--wash`, 판면은 흰색입니다 — 순백 카드는 소셜 피드에서 빈 이미지처럼 보입니다.
- 서체는 Google Fonts를 쓸 수 없어 시스템 폰트로 대체했습니다.
  Malgun Gothic(한글), Consolas(모노), **Georgia italic**(라틴 — `index.html`의
  `--didone` 폴백과 동일하므로 인상 차이가 작습니다). 웹폰트와 완전히 같지는 않습니다.
  Consolas에는 한글 글리프가 없으므로 **모노 라벨에 한글을 넣으면 안 됩니다.**
- 생성 스크립트는 저장소에 넣지 않았습니다. "빌드 과정 없는 단일 파일" 원칙을 지키기 위해서입니다.
  다시 만들 일이 생기면 Pillow로 새로 그려야 합니다
  (2배 슈퍼샘플링 후 LANCZOS 축소해야 얇은 선이 깨지지 않습니다).

> **⚠ 팔레트를 바꾸면 `og.png` 와 아이콘 3개도 다시 만들어야 합니다.**
> 인라인 SVG는 토큰을 상속하지만 PNG는 굽힌 이미지라 자동으로 안 바뀝니다.
> 2026-07-27 봄 팔레트 개편 때도 이 4개 파일을 전부 다시 생성했습니다.
> 안 하면 사이트는 흰색·초록인데 카카오톡 공유 미리보기만 옛 회색·파랑으로 남습니다.

---

## 9. 주의사항

**개인정보** — 문의 폼이나 댓글을 붙이면 이름·이메일·IP 등을 수집하게 됩니다. 개인정보처리방침 페이지를 만들고 수집 항목, 보유 기간, 처리 위탁(외부 서비스 이용 시), 국외 이전(해외 서비스 이용 시)을 명시해야 합니다. 출판사 공식 사이트라면 생략하지 마세요.

**참고 사이트** — 참고 대상으로 언급된 네이버 카페 글(`cafe.naver.com/minholicbook/46952`)은 도구에서 접근이 차단되어 확인하지 못했습니다. 현재 디자인은 카페 글을 참고한 것이 아니라 검색으로 확인한 출판사 정보만 바탕으로 독자적으로 구성한 것입니다. 카페 쪽 레이아웃을 따라가야 한다면 화면 캡처나 설명이 필요합니다.

**저작권** — 기존 imweb 사이트의 문구를 그대로 옮기지 않았습니다. 소개문은 새로 작성한 초안입니다.
